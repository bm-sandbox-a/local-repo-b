{
  "name": "notes-ui",
  "version": "0.1.0",
  "description": "A browser UI for notes-api. No dependencies.",
  "private": true,
  "scripts": {
    "start": "node serve.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
